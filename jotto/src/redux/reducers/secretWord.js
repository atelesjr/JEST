//import { GUESS_WORD } from '../actions'

const secretWord = ( state = null , action ) => {
    return state
    // switch(action.type) {
    //     case (GUESS_WORD): 
    //         return true;
    //     default: 
    //         return state
    // }
}

export default secretWord