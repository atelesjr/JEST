import { GUESS_WORD } from '../actions'

const guessedWord = ( state , action ) => {
    return null
    // switch(action.type) {
    //     case (GUESS_WORD): 
    //         return true;
    //     default: 
    //         return state
    // }
}

export default guessedWord